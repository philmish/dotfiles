- name: Clone nerd-fonts
  tags: setup, neovim, fonts
  git:
    repo: https://github.com/ryanoasis/nerd-fonts
    clone: yes
    depth: 1
    dest: '~/fonts/'

- name: install fonts
  tags: setup, neovim, fonts
  shell: bash ~/repos/github.com/nerd-fonts/install.sh
